// Server configuration (ADVANCED)
{
	// Directory where your blogs are.
	"appDirectory": "blogs/",

	"components": {
		// HTTP server configuration
		"http": {
			// "debug": true,
			// "verbosity": 1,
			"class": "Http",
			"express": true,
			// You can change Blongular listening port here.
			"listen":[27890]
		}
	}
}